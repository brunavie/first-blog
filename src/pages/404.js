import React from 'react';
import Layout from '../components/Layout';

export default () => (
    <Layout>
        <p>
            Infelizmente, a página desejada não foi encontrada.
        </p>
    </Layout>
);
